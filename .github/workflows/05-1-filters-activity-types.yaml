name: "05 - 1 - Event Filters and Activity Types"
on:
  pull_request: 
    types: [opened, synchronize]
    branches:
      - master 

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: "Show the trigger"
        run: echo "Running whenever a PR is opened or synchronized and on base branch is main."