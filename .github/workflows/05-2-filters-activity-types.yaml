name: "05 - 2 - Event Filters and Activity Type"
on:
  pull_request:
    types: [closed]
    branches:
      - master

jobs:
  echo:
    runs-on: ubuntu-latest
    steps:
      - name: "Show the trigger"
        run: echo "Running whenever a PR is closed and on base branch is main."